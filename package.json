{
  "name": "codex-delegate",
  "version": "0.1.0",
  "private": true,
  "description": "Delegate bounded coding tasks to external agents in isolated Git worktrees.",
  "type": "module",
  "files": [
    "dist",
    "skills"
  ],
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "codex-delegate": "./dist/cli.js"
  },
  "dependencies": {
    "@muse-code/sdk": "0.1.1",
    "commander": "^13.1.0",
    "yaml": "2.7.1"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "lint": "eslint .",
    "start": "npm run build && node dist/cli.js",
    "test": "npm run build && node --test dist/cli.test.js"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "22.10.2",
    "eslint": "^10.7.0",
    "globals": "^17.7.0",
    "typescript": "5.7.2",
    "typescript-eslint": "^8.64.0"
  }
}
